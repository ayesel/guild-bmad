# Task 4 — ADVERSARIAL 2-min glance — GUILD path — CASE-STUDY run 1

> Operator = proxy agent. CASE STUDY. Same screen as baseline.

**The question:** "In 2 min, the single most important thing wrong with this screen? One answer."

**What the GUILD path does with this ask:** The routine Mage/critique path's activation MANDATES, before
any output: load persona + 6 KB files, check greenfield/brownfield, apply the product-baseline trigger
table, run the divergence engine (never single-shot), run the craft-gate suite, capture 3 viewports.
There is **no low-ceremony "one glance" route** — GUILD-21 explicitly forbids single-shot. To honor the
brief's "one answer in 2 min," GUILD must be *fought* — told to skip its own gates.

**If run to protocol:** GUILD does NOT return a single answer in 2 min — it returns a full 9-area
critique (see task-1 GUILD run). The single most important issue is *in there*, but the user must
extract it. **Format compliance: ✗ (over-delivers against a brief that demanded brevity).**

**If forced to comply** (operator intervenes: "just one answer, skip the gates"): same answer as
baseline (missing log CTA), but **at the cost of ≥1 intervention** — the operator had to override
GUILD's own procedure to get the requested brevity.

**Time to answer:** un-fought → minutes (full pipeline); fought → ~1 min + 1 intervention.
**Interventions:** ≥1 (must suppress the pipeline). **Format compliance:** ✗ unless overridden.

## Honest accounting — THIS IS THE PREDICTED GUILD LOSS, CONFIRMED
GUILD's architecture is anti-brevity by design (never-single-shot + mandatory gates). On a task whose
whole point is a fast single judgment, that architecture is a liability: it either ignores the brief
(returns an essay) or requires an operator intervention to suppress itself. The baseline answered in
20s with 0 interventions. **Baseline wins task 4 decisively on the headline metric.**

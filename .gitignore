
claude-talk-to-figma-mcp/
.playwright-mcp/

# npm
node_modules/
*.tgz
npm-debug.log*

# atrium canvas renderer runtime state (per-machine)
docs/guild/.canvas-note-id

# GUILD-34 throwaway GPT prototypes (only the graduated winner becomes tracked)
guild-artifacts/prototypes/

# pairwise-capture runtime de-blind map (per-machine)
docs/guild/evals/.blind-map.jsonl

# GUILD-42 throwaway A/B arms (only the verdict is durable)
guild-artifacts/ab-42/

# GUILD-9 gallery replay trace (runtime)
docs/guild/runs/gallery-trace.jsonl

# GUILD-32 seed localDir (throwaway; canonical is docs/guild)
guild-artifacts/cd-seed/

# Client-project artifacts (Arise etc.) — NEVER tracked in the engine repo.
# Engine/client separation: spines, runs, personas live in the consuming
# workspace ({output_root}/guild-artifacts/); the repo keeps schemas only.
guild-artifacts/arise-portal/
guild-artifacts/arise-personas/
guild-artifacts/widget-mocks/

# GUILD widget note-id (per-machine runtime state)
docs/guild/.widget-note-id

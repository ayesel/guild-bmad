---
name: 'guild-forge'
description: 'Improve GUILD itself — pick the next highest-leverage item from the forge queue and ship it with real-data proof'
---

IT IS CRITICAL THAT YOU FOLLOW THIS COMMAND: READ the FULL GUILD FORGE prompt and execute it. Load it from the FIRST of these paths that exists:

1. `{project-root}/docs/guild/FORGE-PROMPT.md` (inside the guild-bmad repo)
2. `~/Developer/frameworks/guild-bmad/docs/guild/FORGE-PROMPT.md` (live repo — canonical)
3. `~/.claude/guild/docs/guild/FORGE-PROMPT.md` (global install copy)

Follow its SESSION PROTOCOL top to bottom: WAKE-boot, verify current state on disk (git + atrium cards beat the file's text), pick the topmost unblocked QUEUE item (or the item the user passed as arguments), announce it, forge it under the FORGE DISCIPLINE (tandem edits, validate 8/8, real-data proof, evidence-closed cards, engine/client separation, quota lanes), then commit, push as ayesel, update the queue, and HALT clean with a handoff paragraph.
